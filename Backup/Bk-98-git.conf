# Configuration file for /var/www/no-ssl/local_scripts/BkRsync-remote-source.pl

              = /etc/Backup/Bk-98-git.conf

Log Base      = /mnt/efs/Bk/Day
Target        = /mnt/efs
Remote        = git-ak20
Source        = /home
Directories   = git

# space separated list of Directories will be synced from Source Base

rsync Options = --verbose --archive --delete --rsh="ssh -q" --exclude='.bash*' --exclude='.cache' --exclude='.config' --exclude='.gnupg' --exclude='.profile' --exclude='RCS' --exclude='.ssh' --exclude='.Xauthority'
